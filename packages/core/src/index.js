/**
 * @aisc/simengine — public exports.
 *
 * Foundation phase placeholder. Real exports land in commits 4 and 5:
 *   - createState        (./engine/state.js)        — commit 4
 *   - createRecorder     (./engine/recorder.js)     — commit 4
 *   - a11y helpers       (./engine/a11y.js)         — commit 5
 *   - particles / graph / controls stubs            — commit 5
 */
export {};
